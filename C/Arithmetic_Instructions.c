#include<stdio.h>

int main() {
    int a = 1, b = 2, c = 3;

    // Valid assignment
    a = b + c;

    // Invalid assignment (commented out)
    // b + c = a;

    printf("%d \n", 3 % 2);   // Output: 1
    printf("%d \n", -3 % 2);  // Output: -1

    return 0;
}
