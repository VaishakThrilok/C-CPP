#include<iostream>
using namespace std;

int main(){

    int a, b, Min;

    cout << "Enter the first number:";
    cin >> a;

    cout << "Enter the second number";
    cin >> b;

    Min = a < b;
    if (Min = a < b){
        cout << "Min of two numbers is :" << a << endl;
    }
    else{
        cout << "Min of two numbers is :" << b << endl;
    }

    return 0;
}